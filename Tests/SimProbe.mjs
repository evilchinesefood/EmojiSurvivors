// Seeded headless full-run probe. Fleshed out from M2 onward (needs the sim).
// Until then it is a no-op gate so `npm test` stays green at the scaffold stage.
console.log("SimProbe: sim not built yet (M0 scaffold) — skipping.");
process.exit(0);
