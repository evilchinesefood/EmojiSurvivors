# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Logs
*.log
npm-debug.log*

# Node (only used as a test runner; no deps expected)
node_modules/

# OMC orchestration state (not part of the game)
.omc/

# Font Awesome / Web Awesome private registry auth (never commit)
.npmrc

# Local agent instructions + planning docs (never deploy/publish)
CLAUDE.md
docs/
fixes.md

# Online backends — deployed via rsync. Leaderboard.php holds the server-side
# signing salt and the JSON stores are live server state: keep those off the
# public repo. Api/Signal.php + Api/.htaccess carry no secrets and ARE tracked.
Api/Leaderboard.php
Api/LeaderboardData.json
Api/SignalData.json
