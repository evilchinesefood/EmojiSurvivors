# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Logs
*.log
npm-debug.log*

# Node (only used as a test runner; no deps expected)
node_modules/

# OMC orchestration state (not part of the game)
.omc/

# Font Awesome / Web Awesome private registry auth (never commit)
.npmrc

# Local agent instructions + planning docs (never deploy/publish)
CLAUDE.md
docs/
fixes.md

# Online backends — the PHP endpoints + .htaccess ARE tracked. The real signing
# salts (Api/Secret.php) and the live JSON data stores stay off the repo.
Api/Secret.php
Api/LeaderboardData.json
Api/SignalData.json

# Paid Fonticons assets — NOT redistributable, so they are not committed. Bring
# your own (see README "Vendored assets"); they stay local for dev + rsync deploy.
Shared/Vendor/FontAwesome/
Shared/Vendor/WebAwesome/
