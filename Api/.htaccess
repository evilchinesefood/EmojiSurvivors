# The leaderboard data store is server state — never serve it raw.
<Files "LeaderboardData.json">
  Require all denied
</Files>

# Co-op signaling store — server state, never serve it raw.
<Files "SignalData.json">
  Require all denied
</Files>
